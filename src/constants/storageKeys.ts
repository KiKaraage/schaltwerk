// Centralized localStorage keys

export const StorageKeys = Object.freeze({
  sessions: Object.freeze({
    sortMode: 'schaltwerk:sessions:sortMode',
    filterMode: 'schaltwerk:sessions:filterMode',
  }),
})
